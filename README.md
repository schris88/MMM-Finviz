# MMM-Finviz
npx playwright install --with-deps
and npm install 

Use like

{
    module: "MMM-Finviz",
    position: "bottom_center",
    config: {
      updateInterval: 2 * 60 * 1000,
      maxWidth: "100%"
    }
},
